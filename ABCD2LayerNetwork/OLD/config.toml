#Network Configuration
numInputActivations = 2                  # Number of input activations
numHiddenLayers = 2                      # Number of hidden layers
numHiddenActivations = [0, 5, 5]       # Number of hidden activations (must have a 0 in front of it and behind it)
numOutputActivations = 3                 # Number of output activations
weightsConfiguration = 0                 # 0 for random, 1 for loading from file, 2 for manual
outputPrecision = 5                      # Number of decimal places to round output to
activationFunction = "sigmoid"           # 0 for sigmoid, 1 for linear

#Training
training = true             # true for training and running, false for just running
numCases = 4                # Number of cases in the training data
maxIterations = 100000      # Maximum number of iterations
lambda = 0.3                # Learning rate
randMin = 0.1               # Minimum random weight
randMax = 1.5               # Maximum random weight
errorThreshold = 2e-4       # Error threshold
savingWeights = false       # true for saving weights to file, false for not saving

#File Configuration
fileWeights = "WeightsI"
saveWeightsFile = "WeightsI"
trainingFile = "TrainingData.txt"
trainingAnswersFile = "TrainingAnswers.txt"
testingFile = "TestData.txt"